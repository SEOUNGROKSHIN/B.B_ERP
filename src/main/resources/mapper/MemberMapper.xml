<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper">

    <!-- 전체 회원 목록 조회 -->
    <select id="findAll" resultType="org.zero.b_b_erp.member.dto.MemberDTO">
        SELECT
            *
        FROM
            ejobb.public.bb_member
    </select>

</mapper>
